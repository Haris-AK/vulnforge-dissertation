% =============================================================================
% CHAPTER 4: Results and Evaluation
% =============================================================================
% SPDX-FileCopyrightText: 2026 Frank Langbein <frank@langbein.org>
% SPDX-License-Identifier: LPPL-1.3c
%
% =============================================================================

The evaluation tests VulnForge as a complete pipeline rather than separate components. It focuses on the complete lifecycle of the application. This is stricter than just checking whether the LLM can produce a vulnerability, or Ansible can deploy a container, as both of those can occur, whilst still having the challenge be unusable.

We address four key questions:
\begin{enumerate}
	\item Can VulnForge generate valid vulnerability specifications across the supported categories and difficulty levels?
	\item Can the platform deploy and inject these specifications into the isolated containers reliably?
	\item Do the generated challenges pass health and flag protections, and exploit verification?
	\item What weaknesses are shown from the project from the evaluation data?
\end{enumerate}

The results show that VulnForge does work as an end to end prototype for the majority of accessed challenges. 57 / 75 full lifecycle evaluations succeeded. This evidence shows that the architecture is viable, however, there is also failures that mean the system is not consistently reliable. The most evident weaknesses are generation failures and validation checks, with some evaluation weaknesses being caused by local infrastructure capacity and AI credit constraints.

\subsection{Evaluation method}\label{sec:eval-method}

The main evaluation was performed using the evaluation harness mentioned in~\ref{sec:evaluation-harness-design} and~\ref{sec:evaluation-harness-implementation}. This script uses the same generation, deployment, injection, verification and clean up as the web app, rather than using a separate mock. This was vital as the testing was meant to show that the project aim of a complete platform was viable, not just a specific module in the pipeline.

The results were the following:

\begin{table}[H]
	\centering
	\caption{Evaluation configuration}
	\label{tab:evaluation-configuration}
	\begin{tabular}{|>{\raggedright\arraybackslash}p{0.34\textwidth}|>{\raggedright\arraybackslash}p{0.56\textwidth}|}
		\hline
		\textbf{Parameter}                & \textbf{Value}                                                                                                     \\
		\hline
		Evaluation mode                   & \lstinline|full|                                                                                                   \\
		\hline
		Categories                        & web server misconfiguration, privilege escalation, SQL injection, insecure file permissions, web application logic \\
		\hline
		Difficulties                      & beginner, intermediate, advanced                                                                                   \\
		\hline
		Runs per category/difficulty pair & 5                                                                                                                  \\
		\hline
		Total planned runs                & 75                                                                                                                 \\
		\hline
		Concurrency                       & 15                                                                                                                 \\
		\hline
		Output artefacts                  & \lstinline|results.json|, \lstinline|results.csv|, \lstinline|summary.md|, generated specifications                \\
		\hline
	\end{tabular}
\end{table}

Each evaluation run uses the following lifecycle:
\begin{enumerate}
	\item Generate a structured vulnerability specification.
	\item Validate the response against the Zod schema and policy checks.
	\item Deploy an Ubuntu 24.04 LXC container with Ansible and Proxmox.
	\item Inject generated files, packages, services and setup commands.
	\item Run the health checks, flag checks and exploit checks.
	\item Record the evidence in JSON, CSV and markdown.
	\item Clean up the container.
\end{enumerate}

This method of evaluation was chosen as it tests the implementation under realistic conditions. A static only generation would just show whether the chosen model can produce valid JSON as per our schema. But it would not show whether the generated challenge was able to be deployed, exploitable and cleaned up. For this run, we used a primary model of Google Gemini 3.1 Pro Preview, and Google Gemini 2.5 pro as a fallback model.

\subsection{Constraints}\label{sec:eval-constraints}

The evaluation was constrained by two external resources, Proxmox host capacity, and AI token credits.

First, full evaluation runs are computationally expensive. Each run may create a container, install packages, write generated files, restart services, run verification commands, and destroy the container. Concurrency had to be capped as we only had a single Proxmox node. The final run used a concurrency value of 15, which was high enough to complete a 75 runs in a practical time but still exposed infrastructure pressure, having one run failed during deployment. Earlier evaluation runs also showed that high concurrency full evaluations can produce VMID collisions or Proxmox API timeouts. Therefore, the concurrency setting should be interpreted as a practical compromise rather than an ideal test conditions.

Secondly, the number of repeated runs was limited by AI cost. Full evaluations require repeated structured generation calls, often with retries. This prevented running a much larger statistical experiment. The final amount of 75 runs is large enough to reveal meaningful reliability patterns, but it should not be treated as a definitive measurement of production reliability.


\subsection{Overall Results}\label{sec:overall-results}

\begin{table}[H]
	\centering
	\caption{Overall full evaluation results}
	\label{tab:overall-results}
	\begin{tabular}{|p{0.55\textwidth}|r|}
		\hline
		\textbf{Metric}              & \textbf{Result} \\
		\hline
		Total runs                   & 75              \\
		\hline
		End-to-end successful runs   & 57/75 (76.0\%)  \\
		\hline
		Failed runs                  & 18/75 (24.0\%)  \\
		\hline
		Generation success           & 72/75 (96.0\%)  \\
		\hline
		Deployment success           & 71/75 (94.7\%)  \\
		\hline
		Injection success            & 71/75 (94.7\%)  \\
		\hline
		Health verification success  & 69/75 (92.0\%)  \\
		\hline
		Flag-protection success      & 65/75 (86.7\%)  \\
		\hline
		Exploit verification success & 61/75 (81.3\%)  \\
		\hline
		Cleanup success              & 71/71 (100.0\%) \\
		\hline
	\end{tabular}
\end{table}

The overall success rate of 76\% shows that the system can complete the full lifecycle for a majority of generated challenges. This is a meaningful result because each successful run required several independently fallible stages to work together: LLM generation, schema and policy validation, container deployment, vulnerability injection, verification, exploit, and cleanup.

The strongest result is cleanup, every deployed container that reached the cleanup stage was successfully destroyed. This is important because VulnForge intentionally creates vulnerable machines. Even when a generated challenge fails, the platform must avoid leaving unnecessary containers running.

The weakest stage was exploit verification, with 61 out of 75 runs passing. This indicates that the main limitation is not basic container orchestration but verifying the exploit, the generated environment and generated validation logic do not always agree on a working exploit path to the flag.

\subsection{Results by category}\label{sec:results-by-category}

\begin{table}[htbp]
	\centering
	\caption{Success by vulnerability category}
	\label{tab:results-by-category}
	\begin{tabular}{|p{0.55\textwidth}|r|}
		\hline
		\textbf{Category}           & \textbf{Success} \\
		\hline
		Web server misconfiguration & 12/15 (80.0\%)   \\
		\hline
		Privilege escalation        & 11/15 (73.3\%)   \\
		\hline
		SQL injection               & 13/15 (86.7\%)   \\
		\hline
		Insecure file permissions   & 10/15 (66.7\%)   \\
		\hline
		Web application logic       & 11/15 (73.3\%)   \\
		\hline
	\end{tabular}
\end{table}

SQL injection was the strongest category, succeeding in 13 out of 15 runs. This suggests that the SQL injection prompt patterns and verification commands were comparatively well aligned. SQL injection challenges often have a deterministic HTTP request that can be used to recover the flag, which makes them easier to verify automatically.

Insecure file permissions was the weakest category, succeeding in 10 out of 15 runs. This category overlaps semantically with privilege escalation and flag protection rules, so it is more difficult to distinguish intended exposure from accidental exposure. For example, a directly readable file might be the intended vulnerability in one design but a security failure in another. This motivated the later use of explicit \lstinline{flag_exposure} in the schema so the evaluator can distinguish intended access from unintended leakage.

Web server misconfiguration performed relatively well overall, but some failures showed that HTTP path exposure is difficult to evaluate. A challenge may legitimately involve discovering an exposed file over HTTP, whereas the evaluator must still reject unintended direct flag disclosure. This again shows why the evaluation needed more than simple string checks.

\subsection{Results by difficulty}\label{sec:results-by-difficulty}

\begin{table}[htbp]
	\centering
	\caption{Success by difficulty}
	\label{tab:results-by-difficulty}
	\begin{tabular}{|p{0.55\textwidth}|r|}
		\hline
		\textbf{Difficulty} & \textbf{Success} \\
		\hline
		Beginner            & 17/25 (68.0\%)   \\
		\hline
		Intermediate        & 21/25 (84.0\%)   \\
		\hline
		Advanced            & 19/25 (76.0\%)   \\
		\hline
	\end{tabular}
\end{table}

The results do not show a simple pattern where harder difficulties always fail more often. Intermediate challenges had the highest success rate, while beginner challenges had the lowest. This suggests that reliability was affected more by the particular generation and validation than by the nominal difficulty label. In other words, the difficulty setting influenced the generated scenario, but it was not the only or dominant factor in whether a challenge deployed and verified successfully.

This is an important critical point, the evaluation supports the claim that VulnForge can generate challenges across difficulty levels, but it does not yet prove that the difficulty labels are pedagogically calibrated. A separate user centred or expert review evaluation would be required to show that beginner, intermediate, and advanced challenges are actually experienced that way by learners.

\subsection{Failure Analysis}\label{sec:failure-analysis}

The script classified failures by stage and by likely cause. These classifications are heuristic, but they make the results more interpretable than a single pass/fail percentage.

\begin{table}[htbp]
	\centering
	\caption{Failure stages}
	\label{tab:failure-stages}
	\begin{tabular}{|p{0.45\textwidth}|r|r|}
		\hline
		\textbf{Failure stage}  & \textbf{Count} & \textbf{Share of all runs} \\
		\hline
		None --- successful run & 57             & 76.0\%                     \\
		\hline
		Verification            & 14             & 18.7\%                     \\
		\hline
		Generation              & 3              & 4.0\%                      \\
		\hline
		Deployment              & 1              & 1.3\%                      \\
		\hline
	\end{tabular}
\end{table}

Most failures occurred during verification. This means the platform usually reached the point where a container existed and a challenge had been injected, but the generated challenge then failed one of the health, flag protection, or exploit checks. This is useful evidence because it narrows the main weakness to semantic correctness and validation reliability rather than orchestration.

\begin{table}[htbp]
	\centering
	\caption{Failure classifications}
	\label{tab:failure-classes}
	\begin{tabular}{|p{0.45\textwidth}|r|r|}
		\hline
		\textbf{Failure classification}      & \textbf{Count} & \textbf{Share of all runs} \\
		\hline
		LLM challenge configuration failure  & 6              & 8.0\%                      \\
		\hline
		Uncertain semantic failure           & 6              & 8.0\%                      \\
		\hline
		Generation policy failure            & 2              & 2.7\%                      \\
		\hline
		Generated validation test failure    & 2              & 2.7\%                      \\
		\hline
		Generation schema failure            & 1              & 1.3\%                      \\
		\hline
		Infrastructure orchestration failure & 1              & 1.3\%                      \\
		\hline
	\end{tabular}
\end{table}


The two largest failure classes were LLM challenge configuration failures and semantic failures. LLM challenge configuration failures occur when the generated challenge does not protect or expose the flag in the intended way. Uncertain semantic failures occur when an exploit check does not recover the flag, but further manual review is needed to decide whether the challenge itself is broken or whether the generated exploit command is brittle.

These results show both a strength and a weakness of the approach. The strength is that the evaluation can identify meaningful failure classes. The weakness is that the system still depends on the LLM not only to generate the vulnerable environment but also to generate validation checks that accurately represent that environment.

\subsection{Evaluation against functional requirements}\label{sec:eval-against-func-req}

The project aim was to produce an MVP rather than to measure learning outcomes with end users. The evaluation therefore combines automated lifecycle evidence from the evaluation harness with developer self validation of user workflows. This is appropriate for the project scope, the critical question is whether the implemented platform satisfies the functional requirements defined in ~\ref{tab:functional-requirements-long}, not whether learners prefer it or learn more from it than from an alternative platform. User studies would be valuable future work, but they are not necessary to validate the MVP against the functional requirements.

The evaluation harness provides the strongest evidence for requirements connected to generation, deployment, injection, verification, and cleanup. Manual developer validation provides evidence for requirements that involve interactive UI behaviour, account state, tutor use, and history pages. Manual validation is weaker than automated testing, but with the constraints of this project, it was deemed a necessary choice.

\begin{longtable}{|p{0.05\textwidth}|p{0.22\textwidth}|p{0.18\textwidth}|p{0.44\textwidth}|}
	\caption{Functional requirements validation}
	\label{tab:requirements-traceability}                                                                                                                                                                                                                                                                            \\
	\hline
	\textbf{FR} & \textbf{Summary}                                                & \textbf{Method}         & \textbf{Evidence and conclusion}                                                                                                                                                                       \\
	\hline
	\endfirsthead
	\hline
	\textbf{FR} & \textbf{Summary}                                                & \textbf{Method}         & \textbf{Evidence and conclusion}                                                                                                                                                                       \\
	\hline
	\endhead
	\hline
	\multicolumn{4}{|r|}{\emph{Continued on next page}}                                                                                                                                                                                                                                                              \\
	\hline
	\endfoot
	\hline
	\endlastfoot

	FR1         & Sign up, log in, log out, remain authenticated.                 & Manual checks           & Account creation, login, page refresh, and logout manually checked. Basic auth flow validated.                                                                                                         \\
	\hline
	FR2         & Store and update an SSH public key.                             & Manual checks           & Settings page tested for key storage and update. Launch route refuses to proceed without a configured key.                                                                                             \\
	\hline
	FR3         & Select vulnerability category and difficulty.                   & Harness + manual checks & Dashboard exposes all categories and difficulties. Final evaluation exercised all five categories and three difficulty levels.                                                                         \\
	\hline
	FR4         & Dynamically generate a vulnerability scenario.                  & Automated harness       & 72/75 runs succeeded (96\%). Strong evidence for generation reliability, though not perfectly consistent.                                                                                              \\
	\hline
	FR5         & Provision a fresh vulnerable environment.                       & Automated harness       & 71/75 runs succeeded (95\%). The single failure was an infrastructure orchestration issue unrelated to the generated challenge.                                                                        \\
	\hline
	FR6         & Inject learner SSH credentials into the environment.            & Harness + manual checks & SSH key injection is part of every deployment; the lifecycle playbook creates the \lstinline{student} user and installs the public key. Supported by 71/75 deployment successes and manual SSH access. \\
	\hline
	FR7         & Inject generated vulnerable content.                            & Automated harness       & 71/75 runs succeeded (94\%), covering files, packages, services, ownership, modes, and setup commands.                                                                                                 \\
	\hline
	FR8         & Verify challenge health before the user sees it.                & Automated harness       & Health verification passed in 69/75 runs (92\%). Failed checks prevented broken challenges from being counted as successful.                                                                           \\
	\hline
	FR9         & Provide sufficient information to access the challenge.         & Manual checks           & Active challenge response includes IP, hostname, SSH user, and a ready-to-copy \lstinline{ssh student@<ip>} command. Validated through the dashboard.                                                  \\
	\hline
	FR10        & Submit a flag and receive feedback.                             & Manual checks           & Correct and incorrect flag submissions manually tested. Correct flag updates challenge status, incorrect flag is rejected.                                                                             \\
	\hline
	FR11        & Prevent indefinite resource use.                                & Harness + manual checks & Cleanup succeeded for 71/71 containers (100\%). Server side expiry and the Proxmox TTL reaper provide two cleanup paths.                                                                               \\
	\hline
	FR12        & Allow the learner to end a challenge manually.                  & Harness + manual checks & Container destruction exercised in all 71 cleanup cases. Manual deletion via the dashboard also validated.                                                                                             \\
	\hline
	FR13        & AI tutor gives guidance without revealing the answer.           & Manual checks           & Tutor tested with hint requests and direct flag requests. Used challenge metadata for relevant hints; unsafe responses were blocked.                                                                   \\
	\hline
	FR14        & Review previous challenge attempts.                             & Manual checks           & History and detail pages manually checked. Previous challenges show status, message counts, solution summaries, and persisted tutor conversations.                                                     \\
	\hline
	FR15        & Support evaluation of generation and orchestration reliability. & Automated harness       & Strongly satisfied. The harness produced JSON, CSV, Markdown summaries, stage timings, failure stages, and heuristic classifications for all 75 runs.                                                  \\
	\hline
\end{longtable}


The requirements evaluation shows that all functional requirements were implemented to MVP level. The strongest evidence is for FR4-FR8, FR11, FR12, and FR15 because these are exercised repeatedly by the automated harness. The weaker evidence is for FR1-FR3, FR9, FR10, FR13, and, FR14 because these were validated manually. That limitation is acceptable for the stated MVP objective, but future work should convert the most important manual checks into automated integration tests.

The most important distinction is that this validation confirms functional behaviour, not educational effectiveness. For example, FR13 is satisfied if the tutor is challenge aware and does not leak the flag or full solution during manual checks. It does not prove that the hints improve learner understanding. Similarly, FR14 is satisfied if previous attempts and tutor history can be reviewed, it does not prove that learners find the review useful. Those questions would require a separate user study or expert review and are outside the MVP evaluation scope.

\subsection{Limitations}\label{sec:eval-limitations}

The evaluation has several limitations. First, the sample size was limited by both compute and AI token credits. More runs would improve confidence, especially for weaker categories such as insecure file permissions and for category and difficulty combinations with only 60.0\% success. Secondly, concurrency affected the evaluation environment. The final run used a concurrency value of 15 to complete in a practical time, but this placed load on the Proxmox host and produced one infrastructure failure. A future evaluation should separate two experiments, a low concurrency correctness run to measure generated challenge quality, and a high concurrency stress run to measure infrastructure capacity. Thirdly, the evaluation does not include a formal user study. It verifies technical solvability, but not learner experience, hint usefulness, or perceived difficulty, however, this was not feasible in the projects timeframe. Future work should include either expert review of generated challenges or small task based user testing. Fourthly, failure classifications are heuristic. They are useful triage labels, but they are not definitive proof of root cause. Manual review remains necessary for uncertain semantic failures.

A further limitation is that the evaluation did not systematically test prompt injection, only manually. This matters because VulnForge uses LLMs both for challenge generation and for the AI tutor. The existing mechanisms do provide some protection, but we do not prove robustness against adversarial prompts. For example, a learner could try to make the tutor ignore its instructions, reveal the solution incrementally, encode the answer, or paraphrase the exploit path without reproducing the exact stored solution text. Therefore, the evaluation demonstrates that the system works under normal expected use, but it does not demonstrate that the LLM components are entirely secure against hostile prompting, only having minor protections.
